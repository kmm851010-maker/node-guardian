CREATE TABLE IF NOT EXISTS pi_news (
  id         TEXT PRIMARY KEY,
  title      TEXT NOT NULL,
  title_ko   TEXT,
  link       TEXT NOT NULL,
  pub_date   TEXT NOT NULL,
  fetched_at TIMESTAMPTZ DEFAULT NOW()
);
